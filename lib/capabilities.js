const Buttons = Object.freeze({
  POWER_ON: 'POWER ON',
  POWER_OFF: 'POWER OFF'
});
const buttons = [{ name: Buttons.POWER_ON }, { name: Buttons.POWER_OFF }];

module.exports = {
  Buttons,
  buttons
};
